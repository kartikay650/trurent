node_modules
.next
out
build
dist

# Local environment files — never commit. Anything matching is excluded
# (Next.js loads .env.local + .env.<env>.local automatically; never these).
.env
.env.*
.env.local
.env*.local
!.env.example

.DS_Store
Thumbs.db
.vercel
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / IDE
.idea
.vscode
*.swp
*.swo

# Scraper / tooling caches
scripts/.cache/
supabase/.temp/

# Test + coverage outputs
coverage
.nyc_output

# Temp + OS junk
*.tmp
*~
